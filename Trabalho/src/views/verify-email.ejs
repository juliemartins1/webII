<!doctype html>
<html lang="pt-BR">
  <head>
    <title>MarketMVP | Verificar e-mail</title>
    <%- include('_head') %>
  </head>
  <body class="font-body bg-brand-50 text-brand-900">
    <div class="min-h-screen grid-bg">
      <header class="border-b border-brand-200/80 bg-white/75 backdrop-blur">
        <div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6 lg:px-8">
          <a href="/" class="font-display text-xl font-bold">MarketMVP</a>
        </div>
      </header>

      <main class="mx-auto flex max-w-7xl items-center justify-center px-4 py-10 sm:px-6 lg:px-8">
        <section class="w-full max-w-md rounded-3xl border border-brand-200 bg-white p-6 shadow-sm">
          <h1 class="font-display text-3xl font-bold">Verifique seu e-mail</h1>
          <p class="mt-1 text-sm text-brand-700">Enviamos um código de 6 dígitos para <strong><%= email %></strong>. Ele expira em 30 minutos.</p>

          <% if (typeof resent !== 'undefined' && resent) { %>
            <p class="mt-3 rounded-xl bg-accent-500/10 px-4 py-2 text-sm font-semibold text-accent-600">Novo código enviado!</p>
          <% } %>
          <% if (typeof error !== 'undefined' && error) { %>
            <p class="mt-3 rounded-xl bg-coral-500/10 px-4 py-2 text-sm font-semibold text-coral-600"><%= error %></p>
          <% } %>

          <form method="POST" action="/verify-email" class="mt-5 space-y-4">
            <label class="block">
              <span class="mb-1 block text-sm font-medium">Código de verificação</span>
              <input type="text" name="code" required maxlength="6" placeholder="123456"
                class="w-full rounded-xl border border-brand-300 px-4 py-2.5 outline-none ring-coral-500 focus:ring tracking-widest text-center text-lg" />
            </label>
            <button type="submit" class="w-full rounded-xl bg-brand-900 px-4 py-2.5 font-semibold text-brand-50 hover:bg-brand-800">Verificar</button>
          </form>

          <form method="POST" action="/resend-code" class="mt-3">
            <button type="submit" class="w-full rounded-xl border border-brand-300 px-4 py-2.5 text-sm font-semibold hover:bg-brand-100">Reenviar código</button>
          </form>
        </section>
      </main>
    </div>
  </body>
</html>
